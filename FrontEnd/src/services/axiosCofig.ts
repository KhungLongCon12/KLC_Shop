import { AxiosInstance } from "axios";
import internalAPI from "./internalAPI";

const apiCall = [];

const axios = axiosInstance.create({
  baseURL: internalAPI.BASE_URL,
  timeout: 30000,
});

axios.intercepter;
